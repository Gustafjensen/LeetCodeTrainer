FROM python:3.11-slim

RUN rm -rf /usr/bin/wget /usr/bin/curl /usr/bin/apt* /usr/bin/dpkg* \
    && useradd -m -s /bin/false runner

WORKDIR /sandbox
RUN chown runner:runner /sandbox

USER runner

CMD ["python3", "/sandbox/solution.py"]
